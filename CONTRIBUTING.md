# Contributing

Protocol changes start in `docs/specification` and require matching schemas,
test vectors and reference-implementation tests. Keep generated and custom code
separate and never patch generated output as the final source of truth.

Do not submit credentials, private IDAX artifacts or operational personal data.
